header.dash-toolbar.dash-toolbar-accent-2
	//(style=(settings.env !== 'production' ? 'background-color: #00B9FF' : ''))
	a.dash-nav-toggler
		span.dash-nav-toggler-icon

	.dropdown.dash-toolbar-dropdown
		a.dropdown-toggle(href="#" data-toggle="dropdown")
			span.d-none.d-md-inline-block=user.username
			span.d-md-none
				span=user.username
		.dropdown-menu.dropdown-menu-right
			a.dropdown-item(href="/users/account") Account
			a.dropdown-item(href="/logout") Logout