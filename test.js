let tap = require('tap')
let createTestDatabase = require('test-database')
let connectMongoose = require('connect-mongoose')

module.exports = {
  assemble() {
    tap.test("Assemble", async tap=> {

    })
  }
}